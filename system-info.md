# pre-build binary installation steps (wsl ubuntu)

ubuntu version
```bash
    lsb_release -a or cat /etc/os-release
```
CPU architecture
```bash
    uname -m or arch
```
kernel information
```bash
    uname -a
```

glibc version
```bash
    ldd --version
```
