LSP features:
    - diagnostics (erros, warnings inline)
    - go-to-definition
    - find references
    - hover docs
    - completion, rename, code actions, references
    - format
    - refactor

Nvim provides LSP client but the servers have to be provded by third parties.
 1. upstream installation (refers to installing software directly from its original source, authors, or maintainers,
    rather than using a version modified or repackaged by a third party.
 2. downstream (in my case :Mason)

